[DEFAULT]
tags = "mtg"
subsuite = "media"
support-files = ["file_voiceschanged.html"]

["test_voiceschanged.html"]
skip-if = [
  "os == 'win' && os_version == '11.26100' && opt && verify-standalone",
  "os == 'win' && os_version == '11.26200' && opt && verify-standalone",
]
