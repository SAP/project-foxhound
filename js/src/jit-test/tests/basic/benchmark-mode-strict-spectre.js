// |jit-test| --strict-benchmark-mode; --spectre-mitigations=on; exitstatus: 1; skip-if: !getBuildConfiguration('benchmark-suitable')
// --strict-benchmark-mode should refuse when spectre mitigations are on.
