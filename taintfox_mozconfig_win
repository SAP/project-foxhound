mk_add_options MOZ_MAKE_FLAGS="-s -j5"
ac_add_options --enable-installer

# Branding: taint branding currently does not work correctly.
# Either choose "official" or "aurora" or use no custom branding at all (results in nightly branding).
# TODO discuss how to deal with the branding in the future
#ac_add_options --with-branding=browser/branding/taint

#
# DEBUG BUILD
#
#mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/obj-ff-dbg
#ac_add_options --enable-debug
#ac_add_options --disable-optimize


#
# RELEASE BUILD
#
mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/obj-tf-release
ac_add_options --disable-tests

