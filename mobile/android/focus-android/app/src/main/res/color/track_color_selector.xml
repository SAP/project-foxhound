<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color when the switch is ON (checked). -->
    <item android:color="@color/accentBright" android:state_checked="true" android:alpha="0.40"/>

    <!-- Color when the switch is OFF (unchecked) -->
    <item android:color="@color/secondaryText" android:alpha="0.30"/>
</selector>
