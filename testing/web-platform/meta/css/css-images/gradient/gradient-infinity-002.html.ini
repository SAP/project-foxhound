[gradient-infinity-002.html]
  expected:
    if not useDrawSnapshot and (os == "linux"): [PASS, FAIL]
    if not useDrawSnapshot and (os == "android"): [PASS, FAIL]
    if not useDrawSnapshot and (os == "win"): [PASS, FAIL]
    if useDrawSnapshot: [PASS, FAIL]
