[DOMMatrix2DInit-validate-fixup.html]
  [addPath({b: 2})]
    expected:
      if (os == "linux") and debug: FAIL
      if os == "win": PASS
      if os == "mac": FAIL
      [FAIL, PASS]

  [addPath({c: 2})]
    expected:
      if (os == "linux") and debug: FAIL
      if os == "win": PASS
      if os == "mac": FAIL
      [FAIL, PASS]

  [addPath({m12: 2})]
    expected:
      if (os == "linux") and debug: FAIL
      if os == "win": PASS
      if os == "mac": FAIL
      [FAIL, PASS]

  [addPath({m21: 2})]
    expected:
      if (os == "linux") and debug: FAIL
      if os == "win": PASS
      if os == "mac": FAIL
      [FAIL, PASS]
