[inline-flex-editing-with-updating-text-crash.html]
  expected:
    if (os == "win") and (processor == "x86") and not debug: [PASS, TIMEOUT, CRASH]
    if (os == "linux") and not debug: [PASS, CRASH]
    if debug and (os == "linux") and (display == "wayland"): [PASS, TIMEOUT]
