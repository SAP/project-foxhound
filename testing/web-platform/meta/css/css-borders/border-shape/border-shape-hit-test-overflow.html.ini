[border-shape-hit-test-overflow.html]
  [CSS Borders Test: hit testing border-shape with overflow]
    expected:
      if (os == "win") and ccov: [PASS, FAIL]
      if not fission and (os == "linux") and debug: [PASS, FAIL]
