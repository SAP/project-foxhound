[clip-path-transition-crash.html]
  expected:
    if not tsan and asan and fission: [PASS, TIMEOUT]
    if tsan: [PASS, TIMEOUT]
    if (os_version == "14") and fission and not swgl: [PASS, TIMEOUT]
    if (os == "android") and not fission and debug and not swgl: [PASS, TIMEOUT]
