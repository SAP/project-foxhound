[image-animation-paused-exact-frame.tentative.html]
  expected:
    if (os == "linux") and not swgl and debug and fission and not useDrawSnapshot: [PASS, FAIL]
    if (os == "linux") and swgl and debug and not fission: [PASS, FAIL]
    if (os == "win") and swgl: [FAIL, PASS]
    if (os == "mac") and debug: [PASS, FAIL]
    if (os == "mac") and not debug: [FAIL, PASS]
    if (os == "android") and not debug: [PASS, FAIL]
