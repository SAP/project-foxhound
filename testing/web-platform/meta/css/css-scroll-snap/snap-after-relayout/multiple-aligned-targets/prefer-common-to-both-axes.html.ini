[prefer-common-to-both-axes.html]
  expected:
    if tsan: [OK, TIMEOUT]
  [scroller prefers target aligned in both axes.]
    expected:
      if tsan: [FAIL, TIMEOUT]
      FAIL

  [scroller follows selected snap target after layout shift, regardless of common snap area.]
    expected:
      if tsan: [FAIL, NOTRUN]
      FAIL
