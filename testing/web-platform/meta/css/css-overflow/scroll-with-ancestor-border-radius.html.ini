[scroll-with-ancestor-border-radius.html]
  expected:
    if asan and fission: TIMEOUT
    if asan and not fission: [OK, TIMEOUT]
  [Wheel-scroll out of rounded corner skips that scroller]
    expected:
      if asan and fission: TIMEOUT
      if asan and not fission: [PASS, TIMEOUT]
