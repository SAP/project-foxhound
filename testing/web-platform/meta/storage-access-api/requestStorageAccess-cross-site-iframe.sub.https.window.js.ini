[requestStorageAccess-cross-site-iframe.sub.https.window.html]
  expected:
    if (os == "mac") and (processor == "aarch64") and not debug: [OK, ERROR, TIMEOUT]
    if (os == "mac") and (processor == "x86_64"): [OK, ERROR, TIMEOUT]
    [OK, TIMEOUT]
  [[cross-site-frame\] document.requestStorageAccess() should be resolved with no user gesture when a permission grant exists, and should allow cookie access]
    expected:
      if (os == "mac") and (processor == "aarch64") and not debug: [PASS, FAIL, NOTRUN]
      if (os == "mac") and (processor == "x86_64"): [PASS, FAIL, NOTRUN]
      if (os == "linux") and not fission: [PASS, FAIL, NOTRUN]
      if (os == "android") and debug: [PASS, FAIL, NOTRUN]
      [PASS, FAIL]

  [[cross-site-frame\] document.requestStorageAccess() should resolve in top-level frame or same-site iframe, otherwise reject with a NotAllowedError with no user gesture.]
    expected:
      if (os == "mac") and not debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "mac") and not debug and (processor == "aarch64"): [PASS, TIMEOUT]
      if (os == "linux") and not fission: [PASS, TIMEOUT]
      if os == "android": [PASS, FAIL, TIMEOUT]

  [[cross-site-frame\] document.requestStorageAccess() should be rejected with a NotAllowedError with denied permission]
    expected:
      if (os == "mac") and not debug and (processor == "x86_64"): [PASS, NOTRUN]
      if (os == "mac") and not debug and (processor == "aarch64"): [PASS, NOTRUN]
      if (os == "linux") and not fission: [PASS, NOTRUN]
      if os == "android": [PASS, FAIL, NOTRUN]
