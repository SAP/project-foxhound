[exit-fullscreen-scroll-to-unscrollable-area-overflow-hidden.html]
  expected:
    if debug and not swgl and (os == "win"): [PASS, FAIL, TIMEOUT]
    if debug and not swgl and (os == "mac"): [TIMEOUT, PASS, FAIL]
    if debug and not swgl and (os == "linux"): [TIMEOUT, PASS, FAIL]
    if debug and swgl: [TIMEOUT, PASS]
    if not debug and (os == "win"): [PASS, TIMEOUT]
    TIMEOUT
