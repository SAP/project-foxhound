[headers.py]
  [test_headers_merged[Custom user context\]]
    expected:
      if (os == "linux") and tsan: [PASS, ERROR]
      if os == "android": FAIL

  [test_headers_merged[Default user context\]]
    expected:
      if tsan: [PASS, ERROR]

  [test_multiple_headers[navigation\]]
    expected:
      if tsan: [PASS, FAIL]
