[pointer_mouse.py]
  [test_down_closes_browsing_context[without up\]]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1998260
    expected:
      if (os == "linux") and not tsan and asan and not fission: [FAIL, PASS]
      if (os == "android") and not debug and fission: [FAIL, PASS]
      if (os == "mac") and (processor == "x86_64"): [FAIL, PASS]
      if (os == "mac") and (processor == "aarch64"): [FAIL, PASS]
      if (os == "linux") and tsan: [FAIL, PASS]
      if (os == "android") and debug: [FAIL, PASS]
      if os == "win": [FAIL, PASS]
      [PASS, FAIL]

  [test_move_to_position_in_viewport[default value\]]
    expected: FAIL

  [test_click_navigation]
    expected:
      if not tsan and asan and not fission: [PASS, FAIL]
      if tsan: [PASS, FAIL]
