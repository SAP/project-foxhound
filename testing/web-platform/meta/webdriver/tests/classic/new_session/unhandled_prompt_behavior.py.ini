[unhandled_prompt_behavior.py]
  expected:
    if (display == "x11") and debug and (os == "linux") and fission: [OK, CRASH]
    if (display == "wayland") and debug: [OK, CRASH]
    if (display == "wayland") and not debug: [OK, TIMEOUT]
