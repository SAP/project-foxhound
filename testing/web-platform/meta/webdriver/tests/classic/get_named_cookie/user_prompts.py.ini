[user_prompts.py]
  expected:
    if (os == "linux") and (display == "wayland") and debug: [OK, TIMEOUT]
  [test_ignore[confirm\]]
    expected:
      if isolated_process and not fission: [PASS, FAIL]
