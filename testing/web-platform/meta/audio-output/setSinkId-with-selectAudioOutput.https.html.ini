[setSinkId-with-selectAudioOutput.https.html]
  expected:
    if (os == "android") and debug and not sessionHistoryInParent: [OK, TIMEOUT]
    if (os == "android") and not debug: [OK, ERROR]
  [setSinkId() on video after play]
    expected:
      if not sessionHistoryInParent and debug: [PASS, TIMEOUT]

  [setSinkId() with deviceID from another window]
    expected:
      if not sessionHistoryInParent and debug: [PASS, NOTRUN]
