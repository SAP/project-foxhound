[streams-close.https.any.worker.html]
  expected:
    if (os == "win") and (processor == "x86_64") and not debug: [CRASH, OK, TIMEOUT]
    if (os == "win") and (processor == "x86"): [OK, TIMEOUT]
    if (os == "mac") and debug: [OK, TIMEOUT]
    if (os == "mac") and not debug: [OK, CRASH, TIMEOUT]
    [OK, TIMEOUT, CRASH]
  [Close and abort unidirectional stream]
    expected: FAIL

  [RESET_STREAM coming from server]
    expected: FAIL

  [Close outgoing stream / bidi-1]
    expected:
      if (os == "linux") and debug and fission: [PASS, FAIL]


[streams-close.https.any.html]
  expected:
    if os == "mac": [OK, TIMEOUT, CRASH]
    [OK, TIMEOUT]
  [Close and abort unidirectional stream]
    expected: FAIL

  [RESET_STREAM coming from server]
    expected: FAIL

  [Close outgoing stream / bidi-1]
    expected:
      if (os == "linux") and not debug and not asan: [PASS, FAIL]
      if (os == "mac") and not debug: [PASS, FAIL]


[streams-close.https.any.sharedworker.html]
  expected:
    if (os == "win") and not debug and (processor == "x86_64"): [CRASH, OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if (os == "mac") and debug: [OK, TIMEOUT]
    if (os == "mac") and not debug: [OK, CRASH, TIMEOUT]
    [OK, TIMEOUT, CRASH]
  [Close and abort unidirectional stream]
    expected: FAIL

  [RESET_STREAM coming from server]
    expected: FAIL

  [Abort unidirectional stream with default error code]
    expected:
      if not fission and not debug: [PASS, FAIL]


[streams-close.https.any.serviceworker.html]
  expected:
    if fission and (os == "win") and not debug and (processor == "x86_64"): [CRASH, OK, ERROR, TIMEOUT]
    if fission and (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if fission and (os == "mac") and debug: [OK, TIMEOUT]
    if fission and (os == "mac") and not debug: [OK, CRASH, ERROR, TIMEOUT]
    if not fission: [OK, ERROR, TIMEOUT, CRASH]
    [OK, ERROR, TIMEOUT]
  [Close and abort unidirectional stream]
    expected: FAIL

  [RESET_STREAM coming from server]
    expected: FAIL
