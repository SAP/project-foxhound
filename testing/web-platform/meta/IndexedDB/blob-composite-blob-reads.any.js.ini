[blob-composite-blob-reads.any.html]
  expected:
    if asan and not fission: [OK, TIMEOUT]
  [Composite Blob Handling: Many blobs: fetch-blob-url]
    expected:
      if asan and not fission: [PASS, TIMEOUT]

  [Composite Blob Handling: Many blobs: direct]
    expected:
      if asan and not fission: [PASS, NOTRUN]


[blob-composite-blob-reads.any.worker.html]
  expected:
    if sessionHistoryInParent and asan and not fission: OK
    if sessionHistoryInParent and not asan: OK
    [OK, TIMEOUT]
  [Composite Blob Handling: Many blobs: direct]
    expected:
      if sessionHistoryInParent and asan and fission: [PASS, NOTRUN]
      if not sessionHistoryInParent and debug: [PASS, NOTRUN]
      if not sessionHistoryInParent and not debug: [PASS, TIMEOUT]

  [Composite Blob Handling: Many blobs: fetch-blob-url]
    expected:
      if not asan and not sessionHistoryInParent and debug: [PASS, TIMEOUT]
      if asan and fission: [PASS, TIMEOUT]
