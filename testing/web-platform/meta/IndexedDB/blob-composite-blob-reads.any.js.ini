[blob-composite-blob-reads.any.html]
  expected:
    if not asan and (processor == "x86") and not debug: [OK, TIMEOUT]
    if asan and not fission: [OK, TIMEOUT]
  [Composite Blob Handling: Many blobs: fetch-blob-url]
    expected:
      if not asan and (processor == "x86") and not debug: [PASS, TIMEOUT]
      if asan and not fission: [PASS, TIMEOUT]

  [Composite Blob Handling: Many blobs: direct]
    expected:
      if not asan and (processor == "x86") and not debug: [PASS, NOTRUN]
      if asan and not fission: [PASS, NOTRUN]


[blob-composite-blob-reads.any.worker.html]
  expected:
    if asan: [OK, TIMEOUT]
  [Composite Blob Handling: Many blobs: direct]
    expected:
      if asan: [PASS, NOTRUN]

  [Composite Blob Handling: Many blobs: fetch-blob-url]
    expected:
      if asan: [PASS, TIMEOUT]
