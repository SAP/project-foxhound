[executeTool-across-trees.https.html]
  expected:
    if (os == "linux") and not tsan and not debug and fission: OK
    if (os == "win") and not debug and (processor == "x86"): OK
    if os == "mac": OK
    if os == "android": OK
    [OK, ERROR]
  [executeTool() rejects when the tool is hosted in another frame tree]
    expected: FAIL
