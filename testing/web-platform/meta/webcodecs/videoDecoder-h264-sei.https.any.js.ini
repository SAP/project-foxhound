[videoDecoder-h264-sei.https.any.html?h264_sei_annexb]
  [H.264 SEI recovery point frames are treated as keyframes.]
    expected:
      if os == "android": PRECONDITION_FAILED


[videoDecoder-h264-sei.https.any.worker.html?h264_sei_annexb]
  [H.264 SEI recovery point frames are treated as keyframes.]
    expected:
      if os == "android": PRECONDITION_FAILED


[videoDecoder-h264-sei.https.any.html?h264_sei_avc]
  [H.264 SEI recovery point frames are treated as keyframes.]
    expected:
      if os == "android": PRECONDITION_FAILED


[videoDecoder-h264-sei.https.any.worker.html?h264_sei_avc]
  [H.264 SEI recovery point frames are treated as keyframes.]
    expected:
      if os == "android": PRECONDITION_FAILED
