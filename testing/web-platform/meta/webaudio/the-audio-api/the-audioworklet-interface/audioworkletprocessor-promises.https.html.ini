[audioworkletprocessor-promises.https.html]
  expected:
    if (os == "android") and debug and sessionHistoryInParent: [OK, CRASH, TIMEOUT]
    if os == "win": OK
    if os == "linux": OK
    [OK, TIMEOUT]
  [test]
    expected:
      if os == "mac": [PASS, TIMEOUT]
