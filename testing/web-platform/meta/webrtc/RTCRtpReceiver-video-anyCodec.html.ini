[RTCRtpReceiver-video-anyCodec.html]
  [An RTCRtpReceiver is prepared to receive any negotiated codec.]
    bug: 1709960
    expected:
      if (os == "mac") and debug: [PASS, FAIL, PRECONDITION_FAILED]
      if (os == "mac") and not debug: [FAIL, PASS, PRECONDITION_FAILED]
      if (os == "linux") and debug: [PASS, FAIL]
      if (os == "linux") and not debug: [PASS, FAIL]
      if os == "android": PASS
      [PASS, PRECONDITION_FAILED]
