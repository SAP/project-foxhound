[RTCRtpTransceiver-stopping.https.html?interop-2026]
  expected:
    if (os == "mac") and debug: [OK, TIMEOUT]
    if (os == "mac") and not debug: [OK, TIMEOUT]

[RTCRtpTransceiver-stopping.https.html?rest]
  expected:
    if (os == "mac") and debug: [OK, TIMEOUT]
    if (os == "mac") and not debug: [OK, TIMEOUT]
