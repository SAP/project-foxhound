[RTCPeerConnection-addTransceiver.https.html?interop-2026]
  expected:
    if (os == "android") and debug and not sessionHistoryInParent: TIMEOUT
    if (os == "mac") and not debug: [OK, TIMEOUT]

[RTCPeerConnection-addTransceiver.https.html?rest]
  expected:
    if (os == "android") and debug and not sessionHistoryInParent: TIMEOUT
    if (os == "mac") and not debug: [OK, TIMEOUT]
