[RTCPeerConnection-addTransceiver-renegotiation.https.html]
  bug: 1709960
  expected:
    if (os == "android") and debug: [OK, TIMEOUT, CRASH]
    if os == "win": OK
    if os == "linux": OK
    [OK, TIMEOUT]
