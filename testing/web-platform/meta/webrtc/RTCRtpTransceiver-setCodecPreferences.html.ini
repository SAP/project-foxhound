[RTCRtpTransceiver-setCodecPreferences.html?interop-2026]
  expected:
    if (os == "android") and debug and sessionHistoryInParent: [OK, TIMEOUT]
    if (os == "android") and debug and not sessionHistoryInParent: [OK, CRASH, TIMEOUT]
    if (os == "android") and not debug and sessionHistoryInParent: [OK, TIMEOUT]
    if (os == "android") and not debug and not sessionHistoryInParent: [OK, TIMEOUT]
  [setCodecPreferences() can remove rtx]
    expected: FAIL

  [setCodecPreferences() filters on receiver and prefers receiver order]
    expected:
      if os == "win": [PASS, FAIL]
      if os == "mac": [PASS, FAIL]

  [setCodecPreferences() should allow setting H264 as first codec]
    expected:
      if os == "win": [PASS, FAIL]
      if os == "mac": [PASS, FAIL]

  [setCodecPreferences() modifies the order of video codecs in createOffer]
    expected:
      if os == "win": [PASS, FAIL]
      if os == "mac": [PASS, FAIL]

  [setCodecPreferences() with only H264 should succeed]
    expected:
      if os == "win": [PASS, FAIL]
      if os == "mac": [PASS, FAIL]

[RTCRtpTransceiver-setCodecPreferences.html?rest]
  expected:
    if (os == "android") and debug and sessionHistoryInParent: [OK, TIMEOUT]
    if (os == "android") and debug and not sessionHistoryInParent: [OK, CRASH, TIMEOUT]
    if (os == "android") and not debug and sessionHistoryInParent: [OK, TIMEOUT]
    if (os == "android") and not debug and not sessionHistoryInParent: [OK, TIMEOUT]
