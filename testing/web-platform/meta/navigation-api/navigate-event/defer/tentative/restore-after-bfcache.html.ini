[restore-after-bfcache.html]
  expected:
    if (os == "win"): [TIMEOUT, OK]
    if os == "mac": TIMEOUT
  [entries() must contain the forward-history page after navigating back to a bfcached page]
    expected:
      if (os == "win"): [TIMEOUT, FAIL]
      if os == "mac": TIMEOUT
      FAIL
