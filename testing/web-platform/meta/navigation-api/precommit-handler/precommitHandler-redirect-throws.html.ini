[precommitHandler-redirect-throws.html]
  expected:
    if os == "mac": [ERROR, OK, TIMEOUT, CRASH]
    if os == "android": [OK, TIMEOUT, CRASH]
    [CRASH, OK, TIMEOUT]
  [redirect() after finish]
    expected: [PASS, FAIL, TIMEOUT]

  [redirect() after commit]
    expected: [PASS, FAIL, TIMEOUT]

  [redirect() traverse]
    expected: [PASS, FAIL, TIMEOUT]
