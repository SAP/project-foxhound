[observer-in-cross-origin-frame.sub.html]
  [Cross-origin observer responds to explicitly set physical size]
    expected:
      if (os == "linux") and not tsan and asan and fission: [PASS, FAIL]
      if (os == "mac") and debug: [PASS, FAIL]
      if (os == "linux") and tsan: [FAIL, PASS]

  [When size is viewport-dependant, cross-origin observer responds to viewport size changes]
    expected:
      if (processor == "x86_64") and not tsan and asan and not fission: PASS
      if (processor == "x86_64") and not tsan and not asan: PASS
      if processor == "aarch64": PASS
      [PASS, FAIL]
