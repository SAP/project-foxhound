[audio-loading-lazy-autoplay-when-visible.html]
  expected: ERROR
  [Audio with loading=lazy and autoplay does not play while not visible, then plays once scrolled into view]
    expected:
      if not debug and (os == "linux") and not asan and not tsan: FAIL
      if not debug and (os == "win"): FAIL
      if not debug and (os == "mac"): FAIL
      TIMEOUT
