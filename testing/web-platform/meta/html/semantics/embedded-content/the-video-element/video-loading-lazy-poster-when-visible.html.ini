[video-loading-lazy-poster-when-visible.html]
  [Video with loading=lazy does not fetch poster while not visible, then fetches poster once scrolled into view]
    expected:
      if (os == "linux") and not tsan and asan and fission: [FAIL, PASS]
      if (os == "linux") and tsan: [PASS, FAIL]
      if (os == "android") and debug: [FAIL, PASS]
      FAIL
