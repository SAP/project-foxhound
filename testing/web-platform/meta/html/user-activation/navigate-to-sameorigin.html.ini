[navigate-to-sameorigin.html]
  expected:
    if not debug and (os == "mac") and (version == "OS X 14.7.5"): [TIMEOUT, OK]
    if debug: [TIMEOUT, OK]
  [User activation propagation across a same-origin navigation]
    expected:
      if (os == "mac") and (version == "OS X 14.7.5"): [TIMEOUT, FAIL]
      if (os == "linux") and debug: [TIMEOUT, FAIL]
      [FAIL, TIMEOUT]
