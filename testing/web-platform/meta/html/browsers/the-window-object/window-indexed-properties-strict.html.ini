[window-indexed-properties-strict.html]
  expected:
    if (os == "linux") and fission and not debug and not asan and tsan: [OK, TIMEOUT]
    if (os == "linux") and fission and not debug and asan: [OK, TIMEOUT]
    if (os == "win") and debug and (processor == "x86_64"): TIMEOUT
    if (os == "linux") and not fission: [OK, TIMEOUT]
