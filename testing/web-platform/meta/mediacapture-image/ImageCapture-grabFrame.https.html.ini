[ImageCapture-grabFrame.https.html]
  expected:
    if (os == "android") and debug and sessionHistoryInParent: TIMEOUT
    if os == "win": OK
    if os == "linux": OK
    [OK, TIMEOUT]
  [exercises the ImageCapture API creation and grabFrame().]
    expected: FAIL
