[pattern-transform-03.svg]
  disabled:
    if useDrawSnapshot and tsan: unknown failure
  expected:
    if (os == "android") and not swgl and debug: [ERROR, PASS]
    if (os == "android") and not swgl and not debug: [PASS, ERROR]
    if (os == "android") and swgl: [PASS, ERROR]
