[pointerevent_movementxy.html?touch]
  [touch pointerevent attributes]
    expected: FAIL


[pointerevent_movementxy.html?pen]
  expected:
    if (processor == "x86_64") and not tsan and (os == "win") and debug: [ERROR, TIMEOUT]
    if (processor == "x86_64") and tsan: [ERROR, TIMEOUT]
    if (processor == "x86") and debug: [ERROR, TIMEOUT]
    ERROR
  [pen pointerevent attributes]
    expected: NOTRUN


[pointerevent_movementxy.html?mouse]
  [mouse pointerevent attributes]
    expected:
      if (os == "win") and not debug: [PASS, FAIL]
