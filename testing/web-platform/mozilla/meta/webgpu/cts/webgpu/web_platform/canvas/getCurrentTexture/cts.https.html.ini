[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:compatibility:*]
  implementation-status: backlog
  [:canvasType="offscreen"]
    expected: FAIL

  [:canvasType="onscreen"]
    expected: FAIL


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:configured:*]
  [:canvasType="offscreen"]

  [:canvasType="onscreen"]


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:expiry:*]
  implementation-status: backlog
  expected:
    if os == "win" and debug: CRASH
    if os == "win" and not debug: ERROR
    if os == "linux" and debug: CRASH
    if os == "linux" and not debug: [OK, ERROR]
    if os == "mac" and debug: CRASH
    if os == "mac" and not debug: ERROR
  [:canvasType="offscreen";prevFrameCallsite="requestAnimationFrame";getCurrentTextureAgain=false]
    expected:
      if os == "win" and debug: FAIL
      if os == "mac" and debug: FAIL

  [:canvasType="offscreen";prevFrameCallsite="requestAnimationFrame";getCurrentTextureAgain=true]
    expected:
      if os == "win" and debug: FAIL
      if os == "mac" and debug: FAIL

  [:canvasType="offscreen";prevFrameCallsite="runInNewCanvasFrame";getCurrentTextureAgain=false]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL

  [:canvasType="offscreen";prevFrameCallsite="runInNewCanvasFrame";getCurrentTextureAgain=true]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL

  [:canvasType="onscreen";prevFrameCallsite="requestAnimationFrame";getCurrentTextureAgain=false]

  [:canvasType="onscreen";prevFrameCallsite="requestAnimationFrame";getCurrentTextureAgain=true]
    expected:
      if os == "win" and debug: [PASS, FAIL]
      if os == "win" and not debug: FAIL
      if os == "linux" and not debug: [PASS, FAIL]
      if os == "mac": FAIL

  [:canvasType="onscreen";prevFrameCallsite="runInNewCanvasFrame";getCurrentTextureAgain=false]

  [:canvasType="onscreen";prevFrameCallsite="runInNewCanvasFrame";getCurrentTextureAgain=true]
    expected:
      if os == "win" and debug: [PASS, FAIL]
      if os == "win" and not debug: FAIL
      if os == "linux" and not debug: [PASS, FAIL]
      if os == "mac": [PASS, FAIL]


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:multiple_frames:*]
  implementation-status: backlog
  expected:
    if debug: CRASH
  [:canvasType="offscreen"]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL

  [:canvasType="onscreen"]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:resize:*]
  implementation-status: backlog
  [:canvasType="offscreen"]
    expected: FAIL

  [:canvasType="onscreen"]
    expected: FAIL


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:single_frames:*]
  implementation-status:
    if os == "win" and debug: backlog
    if os == "linux": backlog
    if os == "mac" and debug: backlog
  expected:
    if os == "win" and debug: CRASH
    if os == "mac" and debug: CRASH
  [:canvasType="offscreen"]
    expected:
      if os == "win" and debug: FAIL
      if os == "linux" and not debug: [PASS, FAIL]
      if os == "mac" and debug: FAIL

  [:canvasType="onscreen"]
    expected:
      if os == "win" and debug: FAIL
      if os == "linux" and debug: [PASS, FAIL]
      if os == "linux" and not debug: FAIL
      if os == "mac" and debug: FAIL


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:usage_as_color_attachment_and_resolve_target:*]
  [:canvasType="offscreen";withView=false;asResolveTarget=false]

  [:canvasType="offscreen";withView=false;asResolveTarget=true]

  [:canvasType="offscreen";withView=true;asResolveTarget=false]

  [:canvasType="offscreen";withView=true;asResolveTarget=true]

  [:canvasType="onscreen";withView=false;asResolveTarget=false]

  [:canvasType="onscreen";withView=false;asResolveTarget=true]

  [:canvasType="onscreen";withView=true;asResolveTarget=false]

  [:canvasType="onscreen";withView=true;asResolveTarget=true]


[cts.https.html?q=webgpu:web_platform,canvas,getCurrentTexture:usage_as_texture_binding:*]
  [:canvasType="offscreen";withView=false]

  [:canvasType="offscreen";withView=true]

  [:canvasType="onscreen";withView=false]

  [:canvasType="onscreen";withView=true]
