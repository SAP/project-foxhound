[MediaStreamTrack-resizeMode.https.html]
  prefs: [media.getusermedia.camera.fake.force:true, media.navigator.streams.fake:false]
  [gDM gets full screen resolution by width]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by true]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"none","width":100}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"none","frameRate":50}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"crop-and-scale"}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"crop-and-scale","height":100}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"crop-and-scale","frameRate":5}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"crop-and-scale","frameRate":50}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"resizeMode":"crop-and-scale","frameRate":5000}]
    expected:
      if os == "android": FAIL
  [gDM doesn't crop with only ideal dimensions]
    expected:
      if os == "android": FAIL
  [gDM doesn't crop with ideal and max dimensions]
    expected:
      if os == "android": FAIL
